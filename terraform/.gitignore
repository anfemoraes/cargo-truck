# Chaves SSH - NUNCA versionar
cargo-truck-key
cargo-truck-key.pub
*.pem

# Valores reais de variaveis (contem IP, chave publica etc.) - versione apenas o .example
terraform.tfvars

# Estado e cache do Terraform - nao deve ir para o git
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
